# the bot for a cave dweller
Multi-platform social automation engine. 

## Features
Watches posts/tweets/channels for keyword triggers in comments/replies. Fires configurable DM/message workflows per platform. Handles follow-gate logic where applicable. Designed so adding a new platform = one new adapter folder, zero core changes.
 
